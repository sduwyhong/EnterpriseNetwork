<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.enterpriseNetwork.dao.admin.AdminDao">
	<insert id="insert">
		INSERT INTO admin
		VALUES
			(#{id},
			#{admin_no},
			#{password},
			#{name},
			#{gender},
			#{age},
			#{email},
			#{enterprise_id})
	</insert>
	
	<insert id="addProduct">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO product
		VALUES
			(#{id},
			#{name},
			#{description},
			#{enterprise_id})
	</insert>
	
	<insert id="addComposition">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO compose_relation
		VALUES
			(#{id},
			#{productId},
			#{compositionId})
	</insert>
	
	<insert id="delegateEnterprise">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO employee_enterprise
		VALUES
			(#{id},
			#{employeeId},
			#{enterpriseId})
	</insert>
	
	<insert id="delegateProduct">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO employee_product
		VALUES
			(#{id},
			#{employeeId},
			#{productId})
	</insert>
	
	<insert id="corporate">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO enterprise_relation
		VALUES
			(#{id},
			#{enterprise_id_1},
			#{enterprise_id_2},
			#{supply},#{stock},
			#{distribution})
	</insert>
	
	<delete id="delete">
		DELETE FROM admin
		WHERE id = #{id}
	</delete>
	
	<delete id="deleteEmployee">
		DELETE FROM employee
		WHERE id = #{employeeId}
	</delete>
	
	<delete id="deleteProduct">
		DELETE FROM product
		WHERE id = #{productId}
	</delete>
	
	<delete id="deleteComposition">
		DELETE FROM compose_relation
		WHERE product_id = #{productId}
		AND composition_id  = #{compositionId}
	</delete>
	
	<delete id="cancelDelegationOfEnterprise">
		DELETE FROM employee_enterprise
		WHERE employee_id = #{employeeId}
		AND enterprise_id = #{enterpriseId}
	</delete>
	
	<delete id="cancelDelegationOfProduct">
		DELETE FROM employee_product
		WHERE employee_id = #{employeeId}
		AND product_id = #{productId}
	</delete>
	
	<delete id="cancelCorporation">
		DELETE FROM enterprise_relation
		WHERE (enterprise_id_1 = #{enterpriseId} AND enterprise_id_2 = #{partnerId})
		OR  (enterprise_id_1 = #{partnerId} AND enterprise_id_2 = #{enterpriseId})
	</delete>
	
	<update id="update">
		UPDATE admin
		SET 
			admin_no = #{admin_no},
			password = #{password},
			name = #{name},
			gender = #{gender},
			age = #{age},
			email = #{email},
			enterprise_id = #{enterprise_id}
		WHERE id = #{id}
	</update>
	
	<select id="get" resultType="Admin">
		SELECT *
		FROM admin
		WHERE id = #{id}
	</select>
	
	<select id="getAll" resultType="Admin">
		SELECT *
		FROM admin
	</select>
	
	<select id="getByNo" resultType="Admin">
		SELECT *
		FROM admin
		WHERE admin_no = #{admin_no}
	</select>
	
	<select id="getEmployees" resultType="Employee">
		SELECT *
		FROM employee
		WHERE enterprise_id = #{enterpriseId}
	</select>
	
	<select id="getProducts" resultType="Product">
		SELECT *
		FROM product
		WHERE product.enterprise_id = #{enterpriseId}
	</select>
	
	<select id="getCompositions" resultType="Composition">
		SELECT cr.id,p1.name product_name,p2.name composition_name
		FROM product p1,compose_relation cr,product p2
		WHERE p1.id = cr.product_id
		AND p2.id = cr.composition_id
		AND p1.enterprise_id = #{enterpriseId}
	</select>
	
	<select id="getEnterpriseDelegations" resultType="EnterpriseDelegation">
		SELECT ee.id,em.name employee_name,en.name enterprise_name
		FROM employee em,employee_enterprise ee,enterprise en
		WHERE em.id = ee.employee_id
		AND en.id = ee.enterprise_id
		AND en.id = #{enterpriseId}
	</select>
	
	<select id="getProductDelegations" resultType="ProductDelegation">
		SELECT ep.id,em.name employee_name,p.name product_name
		FROM employee em,employee_product ep,product p
		WHERE em.id = ep.employee_id
		AND p.id = ep.product_id
		AND em.enterprise_id = #{enterpriseId}
	</select>
	
	<select id="getCorporations" resultType="Corporation">
		SELECT ee.id,en1.id enterprise_id_1,en2.id enterprise_id_2,ee.supply,ee.stock,ee.distribution
		FROM enterprise e1,enterprise_relation er,enterprise e2
		WHERE e1.id = er.enterprise_id_1
		AND e2.id = er.enterprise_id_2
		AND e1.id = #{enterpriseId}
	</select>
</mapper>